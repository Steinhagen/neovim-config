return {
  toggles = {
    dim = true, -- Dims the code outside of your current scope
    git_signs = false,
    diagnostics = false,
    inlay_hints = false,
  },
  show = {
    statusline = false,
    tabline = false,
  },
  win = {
    backdrop = { transparent = false, blend = 90 }, -- How dark the background gets
    width = 120, -- Width of the zen window
  },
}
